# Pillow, pytz and PyYAML are provided by MoviePilot itself. Do not pin them here,
# otherwise fresh MoviePilot installs may reject the plugin for protected runtime conflicts.
numpy>=1.26,<3.0
fonttools>=4.0.0
